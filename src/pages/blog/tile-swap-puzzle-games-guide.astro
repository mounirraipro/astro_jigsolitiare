---
import BlogArticle from "@/components/BlogArticle.astro";
import { articles } from "@/data/articles";

const article = articles.find((item) => item.slug === "tile-swap-puzzle-games-guide");
if (!article) throw new Error("Missing article: tile-swap-puzzle-games-guide");
---

<BlogArticle article={article} />
---
import BlogArticle from "@/components/BlogArticle.astro";
import { articles } from "@/data/articles";

const article = articles.find((item) => item.slug === "pattern-recognition-skills");
if (!article) throw new Error("Missing article: pattern-recognition-skills");
---

<BlogArticle article={article} />
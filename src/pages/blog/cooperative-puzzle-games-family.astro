---
import BlogArticle from "@/components/BlogArticle.astro";
import { articles } from "@/data/articles";

const article = articles.find((item) => item.slug === "cooperative-puzzle-games-family");
if (!article) throw new Error("Missing article: cooperative-puzzle-games-family");
---

<BlogArticle article={article} />
---
import BlogArticle from "@/components/BlogArticle.astro";
import { articles } from "@/data/articles";

const article = articles.find((item) => item.slug === "puzzle-table-setup-small-spaces");
if (!article) throw new Error("Missing article: puzzle-table-setup-small-spaces");
---

<BlogArticle article={article} />
---
import BlogArticle from "@/components/BlogArticle.astro";
import { articles } from "@/data/articles";

const article = articles.find((item) => item.slug === "puzzle-breaks-at-work");
if (!article) throw new Error("Missing article: puzzle-breaks-at-work");
---

<BlogArticle article={article} />
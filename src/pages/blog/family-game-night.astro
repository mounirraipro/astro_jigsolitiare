---
import BlogArticle from "@/components/BlogArticle.astro";
import { articles } from "@/data/articles";

const article = articles.find((item) => item.slug === "family-game-night");
if (!article) throw new Error("Missing article: family-game-night");
---

<BlogArticle article={article} />
---
import BlogArticle from "@/components/BlogArticle.astro";
import { articles } from "@/data/articles";

const article = articles.find((item) => item.slug === "puzzles-and-focus");
if (!article) throw new Error("Missing article: puzzles-and-focus");
---

<BlogArticle article={article} />
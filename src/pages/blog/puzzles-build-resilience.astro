---
import BlogArticle from "@/components/BlogArticle.astro";
import { articles } from "@/data/articles";

const article = articles.find((item) => item.slug === "puzzles-build-resilience");
if (!article) throw new Error("Missing article: puzzles-build-resilience");
---

<BlogArticle article={article} />
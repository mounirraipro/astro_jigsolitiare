---
import BlogArticle from "@/components/BlogArticle.astro";
import { articles } from "@/data/articles";

const article = articles.find((post) => post.slug === "solving-puzzles-together");
if (!article) throw new Error("Missing article: solving-puzzles-together");
---

<BlogArticle article={article} />

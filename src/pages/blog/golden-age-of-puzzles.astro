---
import BlogArticle from "@/components/BlogArticle.astro";
import { articles } from "@/data/articles";

const article = articles.find((item) => item.slug === "golden-age-of-puzzles");
if (!article) throw new Error("Missing article: golden-age-of-puzzles");
---

<BlogArticle article={article} />